import { ResultadoDirective } from './resultado.directive';

describe('ResultadoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResultadoDirective();
    expect(directive).toBeTruthy();
  });
});
